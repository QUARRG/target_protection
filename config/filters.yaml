
FilterGPS:
  # Kalman filter parameters
  P: [0.0001, 0.0001, 0.25, 0.15]
  Q: [0.0, 0.0, 0.0001, 0.0]
  V: [0.1, 0.1, 0.1]
  predict_hz: 100.0
  update_hz: 10.0
  seed: 60
  # Parameters for circle distortion
  embedding_fn_name: modelE
  frame_id: world
  hover_height: 0.3  # meters
  radius_nominal: 0.7  # meters
  omega_nominal: 0.4  # radians per second
  k_phi: 10.0

FilterRelative:
  # Kalman filter parameters
  P_ego: [0.0001, 0.0001, 0.25, 0.15]
  Q_ego: [0.0, 0.0, 0.0001, 0.0]
  V_ego: [0.1, 0.1, 0.1]
  P_rel: [0.0001, 0.0001, 0.25]
  Q_rel: [0.0, 0.0, 0.001]
  V_rel: [0.1, 0.1, 0.1]
  predict_hz: 100.0
  update_hz: 10.0
  seed: 60
  # Parameters for circle distortion
  embedding_fn_name: modelE
  frame_id: world
  hover_height: 0.3 # meters
  radius_nominal: 0.7 # meters
  omega_nominal: 0.4   # radians per second
  k_phi: 10.0

FilterUnicycle:
  # Kalman filter parameters
  P: [1.0, 1.0, 0.15, 0.5, 0.2]
  Q: [0.1, 0.1, 0.01, 0.05, 0.1]
  V: [0.1, 0.1, 0.1]
  predict_hz: 100.0
  update_hz: 100.0
  seed: 60
  # Other parameters
  frame_id: world
  hover_height: 0.3  # meters
  k_phi: 10.0